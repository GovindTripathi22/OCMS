# OCMS Environment Variables
# Copy this file to .env.local and fill in your values.
# Never commit .env.local to source control.

# ─── Database ─────────────────────────────────────────────────────────────────
# SQLite (default, good for local-first mode):
DATABASE_URL="file:./dev.db"

# PostgreSQL (recommended for production):
# DATABASE_URL="postgresql://user:password@host:5432/ocms"

# ─── Auth (Auth.js / NextAuth) ────────────────────────────────────────────────
# REQUIRED. Generate a secure secret:
#   openssl rand -base64 32
AUTH_SECRET=""

# REQUIRED in production. Set to your deployment URL:
#   https://your-app.vercel.app
NEXTAUTH_URL="http://localhost:3000"

# ─── GitHub OAuth ─────────────────────────────────────────────────────────────
# Create an OAuth App at: https://github.com/settings/developers
# Callback URL: http://localhost:3000/api/auth/callback/github
GITHUB_CLIENT_ID=""
GITHUB_CLIENT_SECRET=""

# Webhook secret for GitHub repository push synchronization
GITHUB_WEBHOOK_SECRET=""

# ─── Runtime & Storage Mode ───────────────────────────────────────────────────
# Options: "local" (default) or "production"
STORAGE_MODE="local"

# Absolute path to the root of the project to edit locally.
# Leave blank to use process.cwd() (local dev only).
LOCAL_WORKSPACE_PATH=""

# ─── Optional: Guest / Demo Access ───────────────────────────────────────────
# Set to "true" ONLY in local development to bypass GitHub auth.
# This is ALWAYS disabled in production regardless of this setting.
ALLOW_GUEST_ACCESS="false"

# ─── Optional: SSRF Protection Bypass ────────────────────────────────────────
# Set to "true" ONLY in local dev to allow scraping localhost URLs.
# Never set this in production.
ALLOW_LOCAL_SSRF="false"

# ─── Optional: Online Performance Audits ──────────────────────────────────────
# Google PageSpeed API Key (optional - if omitted, local static audit is used)
PAGESPEED_API_KEY=""
